---
users:
  - name: cwardgar
    group: wheel
    shell: /bin/bash
    ssh_key: id_rsa.pub.cwardgar

  - name: oxelson
    group: wheel
    shell: /bin/bash
    ssh_key: id_rsa.pub.oxelson

  - name: mschmidt
    group: wheel
    shell: /bin/bash
    ssh_key: id_rsa.pub.mschmidt

  - name: mperna
    group: wheel
    shell: /bin/bash
    ssh_key: id_rsa.pub.mperna
